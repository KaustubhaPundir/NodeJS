import path from "path";

import * as url from 'url';
const __dirname = url.fileURLToPath(new URL('..', import.meta.url)); //exporting dirname from utilities to make dirname root directory
export default __dirname