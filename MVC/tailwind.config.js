/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./views/**/*.html"], /**tells tailwind where the html files are to which we have to applied */
  theme: {
    extend: {},
  },
  plugins: [],
}

